import { TColumns } from "./types";

const columns: TColumns = [
    {
        header: "Fault",
        accessorKey: "fault",
    },
    {
        header: "Units",
        accessorKey: "count",
    },
];
export default columns;
